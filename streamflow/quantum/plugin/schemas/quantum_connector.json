{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://streamflow.di.unito.it/plugins/schemas/connector/quantum_connector.json",
  "title": "QSplit Quantum Connector Wrapper",
  "description": "Connector wrapper that selects a quantum provider at runtime and forwards commands to the wrapped connector.",
  "type": "object",
  "properties": {
    "provider": {
      "type": "string",
      "description": "Fallback provider used when auto-selection cannot pick an active backend.",
      "default": "dwave"
    },
    "providerPool": {
      "type": ["array", "string"],
      "description": "List (or comma-separated string) of providers considered by auto-selection.",
      "items": {
        "type": "string"
      }
    },
    "providerServiceMap": {
      "type": "object",
      "description": "Optional mapping provider -> service name, used to route providers to different SLURM services.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "providerServiceFallbackMap": {
      "type": "object",
      "description": "Optional mapping provider -> fallback service list (or comma-separated string), used when a submission is cancelled by the scheduler.",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          {
            "type": "string"
          }
        ]
      }
    },
    "providerEnvMap": {
      "type": "object",
      "description": "Optional mapping provider -> environment variables injected at runtime for that provider.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        }
      }
    },
    "maxConcurrentJobs": {
      "type": "object",
      "description": "Optional mapping provider -> max concurrent jobs. Overrides maxProviderJobs for the given provider.",
      "additionalProperties": {
        "type": "integer",
        "minimum": 1
      }
    },
    "maxProviderJobs": {
      "type": "integer",
      "minimum": 1,
      "description": "If set, limit concurrent jobs per provider (best-effort, within the current StreamFlow process)."
    },
    "transferBufferSize": {
      "type": "integer",
      "minimum": 1,
      "description": "Buffer size for connector transfers.",
      "default": 65536
    }
  },
  "additionalProperties": false
}
